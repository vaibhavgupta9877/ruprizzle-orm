# This workspace defaults to a shared target directory on the G: drive to keep
# the project checkout light.  The path is Windows-specific; on CI or other
# platforms set CARGO_TARGET_DIR to override it.
[build]
target-dir = "G:/cargo-target"

[alias]
xtask = "run -p xtask --"
