[package]
name        = "ruprizzle-testkit"
description = "Dual-database test harness for the ruprizzle ORM (not published)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true
homepage.workspace     = true
readme                 = "README.md"
documentation          = "https://docs.rs/ruprizzle-testkit"
publish                = false

[dependencies]
ruprizzle = { workspace = true }
sqlx      = { workspace = true, features = ["postgres", "sqlite", "mysql", "any"] }
tempfile  = { workspace = true }
thiserror = { workspace = true }
tokio     = { workspace = true }
uuid      = { workspace = true }

[features]
sqlite-rusqlite = ["ruprizzle/sqlite-rusqlite"]
