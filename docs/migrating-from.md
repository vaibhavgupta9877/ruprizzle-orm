# Migrating from other ORMs

The canonical migrating from other ORMs has moved to [MigratingFrom.md](MigratingFrom.md), which is the copy published in the book and the one kept up to date. This stub is preserved so existing links do not break; it is not updated independently.
